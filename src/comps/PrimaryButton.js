import React from 'react';
import './PrimaryButton.css'

const PrimaryButton = ({ children }) => {
    return (
        <>
            <button className="btn41-43 btn-43 mt-5">{children}</button>
        </>
    );
};

export default PrimaryButton;